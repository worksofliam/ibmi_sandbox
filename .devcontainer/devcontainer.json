{
  "customizations": {
    // Configure properties specific to VS Code.
    "vscode": {
      // Set *default* container specific settings.json values on container create.
      "settings": {   
      },
      "extensions": ["halcyontechltd.ibm-i-development-pack"]
    }
  },
  "containerEnv": {
    "VSCODE_IBMI_SANDBOX": "true"
  }
}